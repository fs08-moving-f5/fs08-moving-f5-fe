const AddressChip = () => {
  return (
    <div className="bg-primary-orange-100 text-primary-orange-400 mobile:px-[6px] mobile:text-xs flex items-center justify-center rounded-[16px] px-1 py-[2px] text-sm font-semibold">
      도로명
    </div>
  );
};

export default AddressChip;
